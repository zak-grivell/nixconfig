if not apps[app_id] then
            -- Add the window to the workspace
            local app_item = sbar.add("item", 'app.' .. app_id, {
                display = "active",
                icon = { drawing = false },
                label = {
                    string = app_name,
                    drawing = false,
                },
                background = {
                    drawing = true,
                    image = {
                        scale = 0.75,
                        border_width = 0,
                        string = 'app.' .. app_icon
                    },
                },
                padding_right=5,
                padding_left=5,

                updates = true,
                click_script = 'aerospace focus --window-id ' .. app_id
            })

            table.insert(apps, app_id)

            apps[app_id] = app_item

        end
